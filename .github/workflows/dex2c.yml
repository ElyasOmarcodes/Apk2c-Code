name: Dex2C APK Builder

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Clone Dex2C Tool (dcc)
        run: git clone https://github.com/amimo/dcc.git

      - name: Install Apktool and Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y apktool

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install asn1crypto lxml colorama networkx pydot pycryptodome

      - name: Run Dex2C on APK
        run: |
          # د انډرایډ NDK لاره
          export ANDROID_NDK=$ANDROID_NDK_LATEST_HOME
          
          # فایل د پروګرام فولډر ته کاپي کوو
          cp mohasaba.apk dcc/
          cd dcc
          
          # اصلي عملیه ستاسو د پکسلاب له دقیق پېکج نوم سره
          python dcc.py mohasaba.apk -o protected_app.apk --filter "Lcom/imaginstudio/imagetools/pixellab/.*"
          
          # نوی اپلیکیشن بېرته راوړو
          cp protected_app.apk ../

      - name: Upload Protected APK
        uses: actions/upload-artifact@v4
        with:
          name: Protected-APK-File
          path: protected_app.apk
