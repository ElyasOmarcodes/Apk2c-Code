name: Ultimate APK Protector (Docker)

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Pull Obfuscapk Docker Image
        run: |
          docker pull claudiugeorgiu/obfuscapk

      - name: Run Strong Obfuscation
        run: |
          # دلته مو د -d کمانډ ور اضافه کړ ترڅو فایل خامخا زموږ فولډر ته راوباسي
          docker run --rm -v "${PWD}:/workdir" claudiugeorgiu/obfuscapk -o ConstStringEncryption -o ClassRename -o MethodRename -o FieldRename -o CallIndirection -o Reorder -o Goto -d /workdir/KeyStore_obfuscated.apk /workdir/KeyStore.apk

      - name: Upload Protected APK
        uses: actions/upload-artifact@v4
        with:
          name: Strongly-Protected-APK
          path: KeyStore_obfuscated.apk
